import React from "react";
import { Navbar } from "react-bootstrap";

const Header = () => {
  return (
    <div className="bg-warning  text-center py-2 px-3 rounded">
      <h1>Todo List App</h1>
    </div>
  );
};

export default Header;
